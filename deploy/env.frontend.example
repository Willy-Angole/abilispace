# Copy this to /var/www/shiriki/.env.local on the Droplet

# ✅ Set this to your Droplet IP or domain (with /api/proxy)
NEXT_PUBLIC_API_URL=http://YOUR_DOMAIN_OR_IP

# Google OAuth (same as backend)
NEXT_PUBLIC_GOOGLE_CLIENT_ID=
